// src/lib/featureFlags.ts
// Simple on/off switches for features that are built but not ready to ship.
// Flip back to true when ready — nothing behind these flags is deleted.

export const JOBS_ENABLED = false
